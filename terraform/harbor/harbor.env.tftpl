# Généré par Terraform — valeurs d'installation de Harbor (lu par /opt/harbor-bootstrap.sh)
DOMAIN="${domain}"
LETSENCRYPT_EMAIL="${letsencrypt_email}"
HARBOR_ADMIN_PASSWORD="${harbor_admin_password}"
HARBOR_VERSION="${harbor_version}"
